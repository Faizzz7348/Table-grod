#!/bin/bash
git add src/FlexibleScrollDemo.jsx
git commit -m "Remove border and background from shortcut section icons

- Remove outlined property and border from all shortcut icons
- Set transparent background for all icon buttons
- Add smooth hover effects with scale animation
- Standardize icon sizes to 24px for consistency
- Add custom colors for each icon (cyan, green, gray)"
git push
